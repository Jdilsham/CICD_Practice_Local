apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: cd-listener
spec:
  serviceAccountName: tekton-triggers-example-sa
  triggers:
    - name: cd-trigger
      bindings:
        - ref: cd-binding
      template:
          ref: cd-template
